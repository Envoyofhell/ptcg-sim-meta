# Core Worker Configuration
name = "test-ptcg-sim-meta"
compatibility_date = "2025-03-24"
main = "workers/index.js"

# Environment Variables
[vars]
ALLOWED_ORIGINS = "https://test.meta-ptcg.org,https://test-ptcg-sim-meta.pages.dev,https://meta-ptcg.org,https://ptcg-sim-meta.pages.dev"

# Static Assets Configuration
[site]
bucket = "./client"

# Pages Configuration
[pages]
pages_build_output_dir = "./client"

# Development Environment (default)
# Custom Domain Routes
routes = [
  { pattern = "test.meta-ptcg.org", custom_domain = true },
  { pattern = "test-ptcg-sim-meta.jasonh1993.workers.dev/*", zone_name = "workers.dev" }
]

# Development Database
[[d1_databases]]
binding = "DB"
database_name = "ptcg-sim-meta-db-test"
database_id = "1b70b17d-fa48-4a36-8f02-3698604bdc7f"

# Production Environment Configuration
[env.production]
name = "ptcg-sim-meta"
routes = [
  { pattern = "meta-ptcg.org", custom_domain = true },
  { pattern = "ptcg-sim-meta.jasonh1993.workers.dev/*", zone_name = "workers.dev" }
]

# Production Database
[[env.production.d1_databases]]
binding = "DB"
database_name = "ptcg-sim-meta-db"
database_id = "9da27bfc-4435-44b9-8229-79bc0933e432"

# Durable Objects Configuration
[durable_objects]
bindings = [
  { name = "GAME_ROOM", class_name = "GameRoom" }
]

# Migrations for Durable Objects
[[migrations]]
tag = "v1"
new_classes = ["GameRoom"]

# Assets Binding
# This allows your worker to access static assets
[triggers]
crons = []

[build]
command = "pnpm run build"
watch_dir = "workers"

# Miniflare Development Config
[miniflare]
kv_persist = true
cache_persist = true
durable_objects_persist = true